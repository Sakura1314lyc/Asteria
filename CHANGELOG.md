# Changelog

## 0.12.0

- 首页升级为 Asteria Observatory 深色证据观测台，用真实阶段生成“证据星图”与总体链路读数；项目页使用深色仪器顶栏和浅色研究纸面，减少大面积泛白。
- 重写首页标题、研究账本和状态文案，扩大关键数字与中文标题，保留数据/ID 专用窄体字体，避免整站等宽字与模板式 AI 科技感。
- 手机端重新编排完成度读数、五阶段图、指标与研究档案；继续验证无横向溢出、键盘可达与 `prefers-reduced-motion`。
- 新增 Vercel 公共观测站：冷启动时离线生成确定性 CS 研究样例，跨 Function 实例保持相同项目/运行 ID，服务端拒绝所有非只读请求。
- 公共前端明确显示只读状态、禁用新建研究，并链接本地完整版；不接收 API Key，也不把 Vercel `/tmp` 描述成持久存储。
- FastAPI 依赖提升为核心安装项，新增 `vercel.json`、Vercel 入口、安全忽略规则和线上深链接 QA；生产地址为 `asteria-observatory.vercel.app`。
- UI 演练增加外部项目/运行 URL 注入、HTTP 失败资源定位和控制台来源位置，支持受保护预览与生产环境回归。
- 将路由依赖迁移至 `react-router 8.3.0`，关闭 `GHSA-qwww-vcr4-c8h2`；生产依赖审计为 0 漏洞。

## 0.11.0

- 首页新增由真实项目阶段计算的五通道“证据信号谱”，显示方案、检索、筛选、证据和报告完成比例；扫描只在页面载入时发生，持续脉冲只对应真实运行状态。
- 重新调整为 Circuit Ink、Lab Slate、Spectral Blue 与 Ion Cyan 视觉系统，用深色仪器面打破大面积灰白，同时避免霓虹网格、玻璃拟态和装饰粒子。
- 中文标题改用系统 Display 字体，Bahnschrift/Cascadia Code 仅承担数字、短 ID 和仪器标签，改善中英文混排。
- 主要导航、项目页签、研究档案和运行入口接入 React Router 原生 View Transition；按钮、导航、证据节点与研究账本增加克制的位移反馈。
- 所有新增动态均支持 `prefers-reduced-motion`，Playwright 排练新增移动端横向溢出和减少动态的计算样式断言。
- 新增证据信号推导测试与悬停态截图，保持仪器可视化来自项目状态而非虚构实时活动。

## 0.10.0

- 重塑 Web 为深石墨侧栏、冷灰蓝研究画布和纸张式内容面的 CS 证据工作台；提高标题、正文、控件、列表元数据和运行记录的可读性。
- 首页改为“今日工作”研究账本，并在项目概览新增“方案 → 检索 → 筛选 → 证据 → 报告”证据脊柱，明确当前人工门和下一步。
- 统一常用运行状态、综述类型和事件阶段的中文界面文案；普通按钮默认使用 `type="button"`，避免嵌入表单时意外提交。
- 新增 Playwright 真实页面探索、无交互排练和 WebM 录制脚本；选择器失败时输出可见控件，演示过程不修改研究数据。
- README 新增真实工作台截图、五分钟本地体验、能力—审计依据矩阵、成熟度边界、帮助入口和完整测试命令。
- 项目级锁定并记录 `frontend-design` 与 `ui-demo` 技能来源，便于后续视觉与演示流程复现。
- 新增 wheel 前端资源一致性验证器，阻止 setuptools 历史 `build/lib` 缓存把已删除的哈希资源混入发布包。

## 0.9.0

- 新增 PRISMA-S 导向的逐次检索账本，记录来源、实际查询式、接口、上限、开始/完成时间、耗时、结果数、状态和失败原因。
- 新增 `search_log.json`，同时报告去重前记录、去重后唯一记录、重复项和排序后入选数；协议限制与来源侧限制明确区分。
- 运行研究 API 和 `paper-agent inspect` 暴露检索摘要，旧运行检查点可无损加载新增字段。
- Web 运行页新增非卡片堆叠的检索记录簿、失败来源提示和 JSON 下载，并提高运行日志、阶段与产物列表字号。
- 新增检索故障隔离、历史状态兼容、API 产物和前端可访问性回归测试。

## 0.8.1

- 修复全文筛选跳转到文档页后无法关联当前论文的问题。
- 文档页新增论文选择器；`paper_id` 深链接会预选报告，上传成功后自动刷新全文队列与 PRISMA 计数。
- 新增浏览器组件测试，验证上传请求携带预选论文 ID。

## 0.8.0

- 新增标题/摘要与报告全文两阶段筛选门禁；只有全文最终纳入的研究进入证据提取和综合。
- 新增全文获取状态、文档自动关联、未取得原因和追加式获取事件历史。
- 新增全文单人/独立双人盲审、揭盲、分歧仲裁，以及稳定的主要排除原因代码。
- SQLite schema 升级到 v5；旧 v4 项目自动补齐全文字段，不丢失标题筛选决定。
- 标题候选资格被修订后，当前全文决定自动失效，历史事件和已取得文档继续保留。
- 新增 PRISMA 2020 式运行计数、全文排除原因汇总与 `prisma_flow.json` 项目导出。
- REST API 与 CLI 覆盖全文配置、获取、决定、仲裁、工作区和 PRISMA 查询。
- Web 筛选台新增可深链接的两阶段导航、全文文件台、结构化决定面板和流程计数条。
- 新增真实两阶段端到端运行、旧库迁移、隐私裁剪、事务与 Web API 回归测试。

## 0.7.0

- 新增单人/独立双人筛选模式、双方 reviewer 身份、服务器端盲审隔离与完成后揭盲。
- 新增一致、纳入/排除冲突、含 `maybe` 待讨论、显式仲裁与后续改判使仲裁失效的共识状态机。
- SQLite schema 升级到 v4；当前决定与追加式决定历史、当前仲裁与追加式仲裁历史分开保存。
- 开启双审时自动把既有单人决定迁入第一位 reviewer，不丢弃已完成工作。
- 双审项目只有在所有论文完成双方决定并解决分歧后才能继续证据综合。
- REST API、CLI 和 Web 全部支持配置、个人队列、揭盲、决定对照与仲裁。
- Web 筛选台改为蓝灰研究工作区与论文阅读版式，扩大输入字号和决定区域，并完成桌面浏览器实拍验收。
- 批量筛选改为单一 SQLite 事务，中途失败会完整回滚。
- 项目 ZIP 新增 `screening_audit.json`，保留完整决定与仲裁轨迹。

## 0.6.2

- 修复错误的 `httpx2<1` 约束；开发环境同时覆盖旧版 Starlette 的 `httpx`
  与新版 TestClient 的 `httpx2>=2`，干净环境可正常安装 `.[all,dev]`。
- GitHub Actions 更新到 Node 24 运行时的 checkout、setup-python 与 setup-node 主版本。
- 重新验证公开仓库 CI，而不依赖开发机中已预装的测试包。

## 0.6.1

- 建立正式 GitHub 仓库元数据，补充主页、源码、文档与问题反馈地址。
- 新增 GitHub Actions，持续检查 Python 3.11/3.12、Ruff、pytest、TypeScript、Vitest 与生产前端构建。
- README 新增 CI、Python 与 MIT 许可证状态，公开仓库可直接查看健康度。
- 统一 Python 包、REST API、Web 工作台和测试期望版本为 0.6.1。

## 0.6.0

- 新增 RIS、BibTeX/BibLaTeX 与 CSL JSON 文献库解析，不依赖外部转换服务。
- 新增 Web、CLI 与 REST API 三种批量导入入口，兼容 Zotero、EndNote 和 JabRef 常见导出流程。
- 导入在单个数据库事务内完成，保留稳定证据 ID、已有筛选决定和更完整的元数据。
- 采用 DOI、arXiv ID 与精确规范化标题的保守去重；文件内重复会合并并显式计数。
- 新增 UTF-8/UTF-16/Windows-1252 解码、损坏记录警告、10,000 条记录与上传体积边界。
- 修复通用论文更新可能用不完整结果覆盖既有摘要、作者和引用量的问题。
- 修复圆括号式 BibTeX 的嵌套括号解析，以及文献表格多余列和选中态失效。
- 文献库筛选、表头、标题和元数据字号提升，导入结果显示新增、已存在、补全、重复与跳过数。
- 新增 10 项文献解析、幂等导入、CLI/API 和安全边界回归用例。

## 0.5.5

- 修复规划阶段覆盖用户明确研究问题的问题，并记录输入、精炼结果和采用策略。
- 引用诊断新增可评估覆盖与有效对齐率，跨语言或缺少摘要的引用不再制造虚高满分。
- 离线项目问答保留匹配的全文页码来源，便于无 API 环境验收全文证据链路。
- Web 全文上传保留原始文件名，来源侧栏隐藏内部文档 UUID 并突出文件名与页码。
- 筛选完成后可复核并修改全部决定，同时保留已有决定理由。
- CLI 默认输出可扫读摘要，`--json` 继续提供完整机器数据。
- 报告审计页统一中文标签，并对多数引用不可评估的情况给出显式警告。
- 新增首次用户端到端验收记录与 3 项回归测试。

## 0.5.4

- 重新设计 Asteria 标志，以字母 A 和三个证据节点表达研究问题、论文与全文证据。
- 新增独立 SVG 品牌资产，并同步侧栏 Logo 与浏览器 favicon。
- 新增 `/asteria-mark.svg` 静态品牌资源端点及 API 回归测试。
- 新增颜色、最小尺寸、安全空间和禁用方式等品牌规范。

## 0.5.3

- 直接接入 GitHub Primer Primitives 11.9.0 的 light 与 dark-dimmed 主题。
- 使用深色开发者侧栏、冷灰蓝画布、浅内容面和白色输入面的四级表面层次。
- 重新区分项目头部、表格表头、检查器、运行台和对话三栏背景。
- 保留现有研究组件与交互，避免为组件库重写稳定业务逻辑。
- 完成工作台、设置、项目概览、对话和 390px 移动端视觉验收。

## 0.5.2

- 重构 Web 视觉系统，改为中性、紧凑的计算机科研工作台。
- 首页移除营销式巨幅文案、装饰状态板和网格背景，改为真实项目摘要。
- 统一中文排版、15px 正文、38px 表单控件、5–7px 圆角和克制的阴影规则。
- 项目对话改为会话、正文、来源三栏布局，保留 Agent 与模型连接选择。
- 修复运行阶段列表和证据置信度等动态状态的样式覆盖。
- 完成 1440px 桌面端和 390px 移动端浏览器视觉验收。

## 0.5.1

- 新增 OpenAI 与 DeepSeek 服务商快捷配置。
- DeepSeek 地址会自动使用官方 Chat Completions 接口，避免 `/responses` 404。
- DeepSeek 结构化研究任务改用 `json_object`，并在提示中附带 JSON Schema。
- 连接错误新增 401、402、404 与 429 的可操作中文提示。

## 0.5.0

- 新增五种计算机科研 Agent 配置，并注入计划、证据抽取与报告生成。
- 新增 Responses / Chat Completions 模型连接管理；Web 密钥只驻留进程内存。
- 新增项目级证据对话、SQLite 消息历史与论文/全文来源回链。
- 项目启动和人工筛选后继续均可选择 Agent 与模型连接。
- 全站表单、导航、元数据和对话正文提升字号，删除重复产品说明。
- 前端按页面分包，入口产物由约 509 KB 降至约 262 KB。
- 数据库 schema 升级到 v3，API 与 Python 包升级到 0.5.0。

## 0.4.0

- 新增完整 React/TypeScript Web 工作台，并随 Python wheel 一起分发。
- 新增项目概览、文献三栏库、逐篇筛选台、运行流水线、证据台、图谱、报告和全文检索页面。
- 新增报告、证据、图谱、产物、全文和项目导出的安全读取 API。
- Web 演示模式改用安装包内置合成语料，不再依赖网络检索。
- 文档上传改为分块写入并支持可配置体积限制。
- API 响应移除文档内部绝对存储路径，并增加安全能力摘要。

## 0.3.0

- 专精于计算机科学，采用 arXiv CS taxonomy 与 ACM CCS 上层方向。
- 新增 DBLP 题录检索及官方建议的请求限速。
- 新增算法、系统、安全、软件工程、理论、HCI、机器人和硬件评价协议。
- 新增数据集、基线、指标、消融、算力、代码、效度威胁等 CS 证据字段。
- 新增 CS evidence matrix、benchmark catalog、复现评分和研究议程。
- 评测框架新增 CS 证据完整度与复现报告指标。

## 0.2.0

- 从单次 CLI 原型扩展为多项目科研工作台。
- 新增 SQLite/WAL 数据库、FTS5 全文索引和文档存储。
- 新增系统综述人工筛选暂停与恢复。
- 新增摘要级证据可提取性评价、研究矩阵和文献关系图。
- 新增后台作业、REST API、插件注册、评测和项目导出。
- 工作流由 6 阶段升级为 8 阶段。

## 0.1.0

- 初始论文检索、证据卡、报告和引用审计 CLI。
